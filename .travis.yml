language: python
python:
  - "3.7"
before_install:
  - touch fake_key.pem
  - touch fake_ga_service_account.json
  - mv _secrets.yaml secrets.yaml
install:
  - pip3 install homeassistant
script:
  - hass -c . --script check_config --info all
