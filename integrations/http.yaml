#
# HTTP
#
# https://www.home-assistant.io/integrations/http/
#

http:
  ip_ban_enabled: !secret ip_ban_enabled
  login_attempts_threshold: !secret login_attempts_threshold
  use_x_forwarded_for: true
  trusted_proxies: !secret trusted_proxies
